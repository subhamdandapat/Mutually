import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ShowSkillPage } from './show-skill';

@NgModule({
  declarations: [
    ShowSkillPage,
  ],
  imports: [
    IonicPageModule.forChild(ShowSkillPage),
  ],
})
export class ShowSkillPageModule {}
